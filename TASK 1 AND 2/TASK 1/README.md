# Task 1 – Search Strategy Notebook

This notebook (`pyprogram1.ipynb`) implements the required search‑strategy algorithm for Task 1.  It demonstrates:

- Loading the dataset
- Running the A* (or other) search algorithm
- Visualising the path and performance metrics

**Running the notebook**

1. Activate the virtual environment (see top‑level README).
2. Launch Jupyter and open `pyprogram1.ipynb`.
3. Execute all cells sequentially (`Shift+Enter` or *Run All*).
4. The final cells produce the required plots and tables that answer the assignment questions.

---
*The notebook is self‑contained; no additional files are required beyond the repository contents.*
